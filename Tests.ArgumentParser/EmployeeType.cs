namespace Tests.ArgumentParser;

/// <summary>
/// Shared by every test that exercises EnumArgumentsOfType. The values match the
/// words the test arguments use, so an added value would change existing counts.
/// </summary>
public enum EmployeeType
{
    Production,
    Sales,
    Marketing
}
